Read `app.py` and write a `README.md` that documents the participant's prototype.

The README should cover:

```
# [App Title]

One-sentence description of what this app does.

## What It Does
2-3 bullet points describing the key features

## How to Use
Step-by-step instructions for using the app (written for a non-technical audience)

## Data
Describe any datasets or inputs the app uses (if applicable)

## Built With
- Streamlit
- Pandas
- [any other libraries found in app.py]
```

Guidelines:
- Write for a non-technical audience (e.g. a conference judge or stakeholder)
- Infer the app's purpose and features from the code — do not ask the user
- Keep it concise and clear
- Use the actual app title from `st.title()` or `st.set_page_config()` if present

After writing, say: "✅ README.md updated!"
